apiVersion: v1
kind: Pod
metadata:
  labels:
    run: test
  name: test
spec:
  containers:
  - image: alpine
    name: test
    command: ["/bin/sh", "-c", "printenv"]
    env:
    - name: TEST_ENV_VAR
      value: "Hello, World!"